# Kế hoạch Review Security — Hello World (Contract Management)

> **Cập nhật lần cuối:** 2026-06-12
> **Trạng thái:** ✅ Đã xong · 🔄 Đang làm · ⬜ Chưa làm
>
> Quy ước: khi bắt đầu một bước, đổi ⬜ → 🔄; khi hoàn thành, đổi 🔄 → ✅ và ghi chú kết quả
> (OK / phát hiện vấn đề + link file) vào cột "Kết quả" hoặc mục **Findings** cuối file.

## Tổng quan hệ thống (đã khảo sát)

- **Stack:** React 19 + Vite (SPA, không router lib) · Express 5 · PostgreSQL (pg.Pool) · multer upload
- **Auth:** HMAC-SHA256 session token tự viết ([server/auth/token.js](server/auth/token.js)), cookie httpOnly `tecapro_auth`, gate toàn bộ API bằng `requireAuth` ([server/routes/index.js:32](server/routes/index.js#L32))
- **Bề mặt tấn công chính:** ~20 routers / ~25 controllers REST, upload file đa nơi (contracts, contract-ins, tasks), serve file tĩnh `/uploads` + `/api/files/:id/view`, login công khai
- **Đã có sẵn:** login rate-limit, upload extension blocklist, CORS allowlist, error handler ẩn stack ở prod, denylist token khi logout

---

## Giai đoạn 0 — Khảo sát & lập kế hoạch

| # | Bước | Trạng thái |
|---|------|-----------|
| 0.1 | Đọc cấu trúc dự án, xác định bề mặt tấn công (routes, middleware, upload, auth) | ✅ |
| 0.2 | Lập kế hoạch review chi tiết (file này) | ✅ |

## Giai đoạn 1 — Secrets & cấu hình môi trường ✅ (xong phần kiểm tra local; còn 2 việc thủ công trên VPS)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 1.1 | Kiểm tra `.env` KHÔNG bị commit vào git; xác nhận `.gitignore` cover `.env*` | ✅ | **PHÁT HIỆN F-01 (Critical):** `.env` từng bị commit và vẫn còn trong history (5 commit, mới nhất `cfa456b`); repo `vinhtdcnhn-stack/tecapro` là **PUBLIC** trên GitHub → `TELEGRAM_BOT_TOKEN` đang lộ công khai. Hiện tại `.env` đã được ignore đúng (`.gitignore:15`), working tree sạch. |
| 1.2 | Đánh giá độ mạnh `AUTH_SECRET`; xác nhận VPS dùng secret KHÁC local | ✅ | `AUTH_SECRET` **chưa từng** xuất hiện trong git history (an toàn về mặt lộ lọt). Tuy nhiên giá trị hiện tại chỉ 32 ký tự dạng "human-pattern", ngắn hơn khuyến nghị 48-byte random của chính code ([token.js:11](server/auth/token.js#L11)) → F-02 (Medium). VPS dùng secret khác hay không: kiểm tra thủ công (xem mục VPS cuối giai đoạn). |
| 1.3 | Quét secrets hard-code trong source `server/` + `src/` | ✅ | Sạch — không có password/secret/api-key/token hard-code. `.env.example` chỉ chứa placeholder. |
| 1.4 | Kiểm tra `DATABASE_URL`: mật khẩu DB, DB không expose internet | ✅ (local) / 🟡 (VPS) | Local: `postgres:123456@localhost` — password yếu nhưng chỉ localhost dev → F-03 (Low). Giá trị này cũng nằm trong history public (chỉ là cred localhost, rủi ro thấp). VPS: cần kiểm tra thủ công. |
| 1.5 | Token Telegram lấy từ env, nội dung gửi không nhạy cảm | ✅ | [telegram.js:1](server/services/telegram.js#L1) đọc `process.env.TELEGRAM_BOT_TOKEN` đúng cách, không hard-code. Chỉ gửi thông báo đăng nhập tới `telegram_chat_id` của chính user. Nhưng bản thân token đã lộ qua F-01 → phải revoke. |
| 1.6 | Xác nhận `NODE_ENV=production` trên VPS | 🟡 | Không kiểm tra được từ máy local — xem checklist VPS bên dưới. |
| 1.7 | (Phát sinh) Soát chất lượng file `.env` local | ✅ | `.env` có **khóa trùng lặp**: `VITE_API_BASE_URL` ×2, `DATABASE_URL` ×2 (dotenv lấy giá trị khai báo sau → đang chạy `hello_web_db`, đúng theo memory) → F-04 (Info, dọn dẹp). Thiếu `NODE_ENV`, `ALLOWED_ORIGINS` so với `.env.example` (dev nên chấp nhận được). |

**Checklist thủ công trên VPS (SSH vào chạy, không làm được từ máy local):**

```bash
# 1.2/1.6 — secret & NODE_ENV (trong thư mục app trên VPS)
grep -E '^(AUTH_SECRET|NODE_ENV|DATABASE_URL|ALLOWED_ORIGINS)=' .env   # AUTH_SECRET phải KHÁC local, NODE_ENV=production
# 1.4 — Postgres chỉ listen localhost
ss -tlnp | grep 5432        # mong đợi 127.0.0.1:5432, KHÔNG phải 0.0.0.0
```

## Giai đoạn 2 — Xác thực (Authentication) ✅ Đã xong

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 2.1 | Review [server/auth/token.js](server/auth/token.js): verify dùng `timingSafeEqual` ✓, exp check ✓ — kiểm tra thêm: payload có thể bị tái dùng sau khi user bị khoá/đổi role không (token sống 7 ngày, không có cơ chế invalidate khi đổi quyền) | ✅ | Ký/verify chuẩn (length-check + `timingSafeEqual`, exp check, fail ở prod nếu thiếu secret). **Xác nhận nghi vấn → F-05 (Medium):** `requireAuth` lấy `role` từ payload, không đối chiếu DB — đổi role/xoá user/đổi mật khẩu KHÔNG vô hiệu token cũ (sống tới 7 ngày). Ghi chú thêm: `readToken` chấp nhận cả header `Bearer` ngoài cookie — chỉ là đường vào thứ hai cùng cơ chế verify, chấp nhận được. |
| 2.2 | Review [server/controllers/authController.js](server/controllers/authController.js): login flow — so sánh password bằng bcrypt, không lộ "email tồn tại / sai password" khác nhau (user enumeration), không log password | ✅ | `bcrypt.compare` ✓; cùng một message "Sai email hoặc mật khẩu." cho cả 2 nhánh ✓; không log password ✓. Còn **timing oracle nhẹ**: email không tồn tại → trả lời ngay (bỏ qua bcrypt ~100ms) → đo thời gian đoán được email tồn tại → F-08 (Info, đã có rate-limit che bớt). Các endpoint `check-email`/`check-username` đều `requireAdmin` ✓ nên không phải vector enumeration. |
| 2.3 | Kiểm tra cookie: `httpOnly` ✓, `sameSite: 'lax'` ✓, `secure` theo NODE_ENV ✓ — xác nhận `maxAge` khớp token exp, và path không quá rộng nếu cần | ✅ | `maxAge = TOKEN_MAX_AGE_SEC * 1000` khớp đúng exp của token ✓ ([authController.js:16-25](server/controllers/authController.js#L16-L25)). `path: '/'` là cần thiết (cookie phải đi cùng cả `/api` lẫn `/uploads`) — không quá rộng. OK. |
| 2.4 | Review change-password ([routes/index.js:42](server/routes/index.js#L42)): có yêu cầu mật khẩu cũ không? Độ dài/độ mạnh mật khẩu mới có được enforce? Đổi mật khẩu có revoke các session cũ không? | ✅ | Yêu cầu `current_password` ✓ (admin reset hộ đi qua `updateUser` riêng, hợp lý). Độ dài tối thiểu chỉ **6 ký tự**, không có yêu cầu độ phức tạp → F-07 (Low). Đổi mật khẩu **không revoke session cũ** — token cũ vẫn dùng được tới 7 ngày → gộp vào F-05. |
| 2.5 | Review login rate-limit ([server/middleware/loginRateLimit.js](server/middleware/loginRateLimit.js) + [rateLimit.js](server/middleware/rateLimit.js)): ngưỡng hợp lý, `trust proxy` chỉ tin 1 hop ✓ — thử nghĩ cách bypass (X-Forwarded-For giả khi không qua nginx?) | ✅ | Ngưỡng 10 lần SAI / 15 phút / IP hợp lý; chỉ đếm fail nên không khoá nhầm văn phòng NAT ✓. **Tìm ra cách bypass → F-06 (Medium):** `app.listen(port)` bind `0.0.0.0` + `trust proxy 1` — nếu firewall VPS không chặn port 5174, attacker gọi thẳng Express (không qua nginx) và tự đặt `X-Forwarded-For` → mỗi request một "IP" mới, vô hiệu rate-limit. Cần bind `127.0.0.1` ở prod hoặc xác nhận firewall chặn (thêm vào checklist VPS). Bộ đếm trong RAM (mất khi restart, không share đa instance) — chấp nhận với 1 instance. |
| 2.6 | Kiểm tra denylist logout chỉ trong RAM (mất khi restart) — đánh giá rủi ro chấp nhận được với app nội bộ, ghi nhận vào findings nếu cần Redis | ✅ | Chấp nhận được: denylist chỉ là lớp phòng vệ phụ (cookie httpOnly đã bị xoá khi logout), có tự dọn mỗi giờ chống phình RAM ✓. Chỉ cần Redis khi chạy nhiều instance — ghi nhận, không cần vá. |
| 2.7 | Kiểm tra dư thừa: cả `bcrypt` lẫn `bcryptjs` trong package.json — xác định bản nào thực dùng, gỡ bản kia | ✅ | Chỉ `bcryptjs` được import (duy nhất tại [authController.js:1](server/controllers/authController.js#L1)); `bcrypt` (native) không dùng ở đâu. **Đã gỡ** `bcrypt` bằng `npm uninstall bcrypt` (−3 packages) → F-09 (Info, **đã vá**). 9.4 coi như xong luôn. |

**Bổ sung checklist VPS (phát sinh từ 2.5 / F-06):**

```bash
# F-06 — xác nhận port Express KHÔNG truy cập được từ ngoài (chỉ nginx gọi vào)
ss -tlnp | grep 5174        # nếu 0.0.0.0:5174 → cần firewall chặn 5174 hoặc đổi app.listen(port, '127.0.0.1')
```

## Giai đoạn 3 — Phân quyền (Authorization / IDOR) ✅ Đã xong (F-10 + F-11 đã vá)

> Nguyên tắc đã đặt trong CLAUDE.md: **identity luôn lấy từ `req.user`, không bao giờ từ client**. Cần verify từng controller có tuân thủ.

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 3.1 | Grep toàn bộ `server/controllers/` tìm chỗ còn đọc `userId` / `user_id` từ `req.body` / `req.query` / `req.params` để quyết định quyền | ✅ | Hầu hết controllers lấy identity từ `req.user` đúng chuẩn ✓ (`created_by`/`uploaded_by` trong documentController, taskAttachmentController; lọc danh sách HĐ trong contractController). **Ngoại lệ duy nhất → F-10 (Medium, đã vá):** [pmDashboardRoutes.js](server/routes/pmDashboardRoutes.js) nhận `:userId` từ URL không có guard. |
| 3.2 | Kiểm tra IDOR theo contract: user thường có xem/sửa được contract không thuộc về mình không? (contractController, contractInController — có check membership `contract_out_member` không, hay chỉ cần đăng nhập là truy cập mọi hợp đồng?) | ✅ | **Xác nhận nghi vấn → F-11 (High):** chỉ DANH SÁCH (`getAllContracts`) lọc theo PM membership cho non-admin; còn `GET /contracts/:id`, `PUT /contracts/:id` và toàn bộ tài nguyên con (BOQ, receivable, progress, guarantee, task, contract-in + mọi bảng con) **không check membership** — chỉ cần đăng nhập là đọc/sửa mọi HĐ theo id. Nghiêm trọng nhất: `PUT /contracts/:id` cho phép user thường thay cả danh sách thành viên (tự thêm mình làm PM). |
| 3.3 | Kiểm tra IDOR tài liệu: `/api/files/:id/view` + documentController — quyền xem file có gắn với quyền xem contract chứa nó không | ✅ | Không gắn — `/api/files/:fileId/view`, `/download` và `DELETE /files/:fileId` chỉ cần đăng nhập, truy cập theo `fileId` bất kỳ (kể cả xoá folder cascade kèm xoá file trên đĩa). Thuộc phạm vi F-11. `file_path` chỉ từ DB (server tự ghi) ✓ — normalize/prefix-check để bước 5.3. |
| 3.4 | Kiểm tra các op admin-only: tạo/sửa user, xoá dữ liệu — route nào thiếu `requireAdmin`? Đối chiếu từng `router.delete`/`router.put` trong `server/routes/*.js` | ✅ | User-management đã gate đủ (`requireAdmin`/`requireSelfOrAdmin` trong [routes/index.js:38-46](server/routes/index.js#L38-L46)) ✓. Ngoài đó, **không một route nào** trong 19 file `routes/*.js` dùng `requireAdmin` — mọi `delete`/`put` (xoá contract-in, bulk-delete BOQ/serial, xoá folder...) mở cho mọi user đăng nhập. Theo thiết kế hiện tại quyền nên theo membership chứ không phải admin → gộp vào F-11, quyết định mô hình ở Giai đoạn 12. |
| 3.5 | Kiểm tra mass-assignment: update user ([authController.updateUser](server/controllers/authController.js)) có cho phép tự set `role = 1` qua body không (kể cả qua change-password / self-update path) | ✅ | An toàn: `changePassword` (self-or-admin) chỉ update `password_hash`, không nhận `role`; `createUser`/`updateUser` nhận `role` nhưng đều `requireAdmin`; `getCurrentUser` lấy id từ phiên; không có self-update profile path nào khác. Bonus: `getAllUsers` ẩn email/sđt/mã NV/telegram/username với non-admin ✓. |
| 3.6 | Kiểm tra warrantyLookupController — đây có phải route tra cứu public/semi-public không, lộ dữ liệu gì | ✅ | KHÔNG public — `/warranty-lookup` mount sau `requireAuth` ✓. Trả serial → HĐ bán/nhập + tên khách hàng/NCC + hạn bảo hành cho **mọi user đăng nhập** (tra cứu cross-contract có chủ đích, chấp nhận với app nội bộ — nhưng lưu ý nếu sau này siết F-11 thì đây là ngoại lệ cố ý). `replaceSerial`/`replaceDeliverySerial` cũng không check membership — thuộc phạm vi F-11. |

## Giai đoạn 4 — SQL Injection ✅ Đã xong (không phát hiện lỗ hổng)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 4.1 | Grep toàn bộ `server/` tìm string interpolation trong SQL: `` query(` `` + `${`, `pool.query(.*\+`, `' || '` — mọi query phải dùng tham số `$1, $2...` | ✅ | Sạch. Mọi chỗ nội suy `${}` / nối `+` trong SQL đều là **hằng số server-side** (`BASE_SELECT`, `WARRANTY_SELECT`, `SELECT_FIELDS`, `IN_PROGRESS_SELECT`) hoặc placeholder `$n` sinh từ index ([contractMemberController.js:29](server/controllers/contractMemberController.js#L29), [contractInCustomsController.js:46](server/controllers/contractInCustomsController.js#L46)); dữ liệu client luôn đi qua mảng params. Query duy nhất ngoài controllers ([middleware/auth.js:37](server/middleware/auth.js#L37), health-check [index.js:46](server/index.js#L46)) cũng tham số hóa/tĩnh ✓. |
| 4.2 | Soát kỹ các chỗ build SQL động: ORDER BY / sort column, filter, search (`ILIKE`), bulk insert (warrantyImportController, BOQ import) — cột sort phải qua allowlist, không nối trực tiếp từ client | ✅ | Không có ORDER BY/sort/search (`ILIKE`) nào nhận từ client — mọi ORDER BY đều hard-code. Các chỗ build SQL động đã soát từng cái: `pmFilter` ([contractController.js:15](server/controllers/contractController.js#L15)) dùng `$1` ✓; `UPDATE ${table} SET ${sets}` ([warrantyController.js:184](server/controllers/warrantyController.js#L184)) — `table` do 2 wrapper server truyền cứng, `sets` là chuỗi cố định ✓; `${table}` trong [serialUtils.js](server/controllers/serialUtils.js) — cả 17 call site chỉ truyền hằng `TABLE_DELIVERY`/`TABLE_EQUIPMENT` ✓; `${FIELDS.join()}` (customs) là mảng cứng ✓. Bulk insert (warrantyImportController, importComponentSerials, BOQ save-all, bulkDeleteBOQ `ANY($1::bigint[])`, bulkDeleteSerials `ANY($1::int[])`) đều tham số hóa ✓. |
| 4.3 | Kiểm tra `LIMIT`/`OFFSET` phân trang có ép kiểu số không | ✅ | App **không có phân trang** từ client — không tồn tại `OFFSET`, mọi `LIMIT` đều là `LIMIT 1` hard-code trong SQL tĩnh (contractInDeliveryController, warrantyImportController, contractInLogisticsController, progressController). Không có vector. |

## Giai đoạn 5 — Upload & phục vụ file ✅ Đã xong (F-12, F-13 đã vá; F-14 chờ 9.2)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 5.1 | Review [server/middleware/uploadFilter.js](server/middleware/uploadFilter.js): hiện là **blocklist** extension — đánh giá chuyển sang allowlist; kiểm tra bypass double-extension (`file.pdf.exe` bị chặn ✓ vì lấy ext cuối, nhưng `file.exe.pdf` lọt — chấp nhận?); chưa kiểm tra MIME/magic bytes | ✅ | Quyết định **giữ blocklist** (app nội bộ nhận đa dạng tài liệu thật, allowlist dễ chặn nhầm; đã có nosniff + requireAuth làm lớp phụ). Nhưng phát hiện blocklist **sót đuôi render được như HTML/SVG** theo mime-db: `.shtml`, `.xht`, `.svgz` → stored XSS → **F-12 (Medium), đã vá**. Double-extension: `file.pdf.exe` chặn ✓; `file.exe.pdf` lọt nhưng chấp nhận — server không thực thi file, trình duyệt nhận mime pdf + nosniff. Không check MIME/magic bytes — chấp nhận: ext (thứ quyết định Content-Type khi serve) mới là lớp đúng cho rủi ro render. |
| 5.2 | Path traversal khi LƯU: filename do multer sinh + sanitize `[^a-zA-Z0-9._-]` ([documentController.js:19-23](server/controllers/documentController.js#L19-L23)) — verify `contractId` trong đường dẫn thư mục được ép kiểu số (hiện `String(req.params.contractId)` — nếu là `../../x` thì sao?) | ✅ | **Xác nhận nghi vấn → F-13 (Medium), đã vá:** Express decode `%2F` trong param nên `contractId = ..%2F..%2Fx` đi thẳng vào `path.join` → tạo thư mục/ghi file tạm NGOÀI `uploads/` (file bị unlink khi controller lỗi nhưng thư mục còn lại). Đã ép `^\d+$` cho `contractId`/`contractInId`/`taskId` ngay trong multer destination; sanitize thêm phần extension của tên file (trước chỉ sanitize base). |
| 5.3 | Path traversal khi ĐỌC: [documentController.js:69](server/controllers/documentController.js#L69) `path.join(__dirname, '..', storedPath)` rồi `res.sendFile` — verify `storedPath` chỉ từ DB (server tự ghi) và vẫn nên normalize + check prefix `uploads/` trước khi sendFile | ✅ | `storedPath` chỉ từ DB do server tự ghi ✓ (đã xác nhận ở 3.3). Vẫn thêm hardening như đề bài: `resolveFilePath` giờ `path.resolve` + chặn prefix ngoài `uploads/` (ném lỗi), áp cho view/download/delete file HĐ; `deleteAttachment` (task) cũng prefix-check trước khi unlink. Gộp vào F-13. |
| 5.4 | Kiểm tra tất cả các điểm upload khác (taskAttachmentController, warrantyImportController nhận file Excel) dùng cùng filter + limits | ✅ | taskAttachmentController dùng đúng `safeUploadFilter` + `UPLOAD_LIMITS` 50MB ✓. 3 điểm import Excel (boqController 10MB, contractInBOQController 10MB, contractInDeliveryController 5MB) dùng memoryStorage + chỉ nhận `.xlsx/.xls`, không ghi đĩa → không cần blocklist chung ✓. warrantyImportController KHÔNG nhận file — nhận JSON đã parse phía client ✓. Grep `multer` toàn `server/`: không còn điểm upload nào khác. |
| 5.5 | Header khi serve file: `/uploads` đã có `nosniff` ✓ — kiểm tra `/api/files/:id/view` có set `nosniff` + `Content-Disposition` phù hợp không (tránh HTML/SVG render inline → XSS) | ✅ | `/uploads` static nosniff ✓; `/api/files/:id/view` ([documentController.js](server/controllers/documentController.js)) và `/download` đều set nosniff ✓; download dùng `res.download` (Content-Disposition: attachment) ✓. View là inline có chủ đích (preview); rủi ro HTML/SVG inline đã chặn từ gốc (F-12) + bổ sung: `viewFile` ép tải về mọi file có đuôi thuộc blocklist để vô hiệu cả **file cũ** upload trước khi có filter. |
| 5.6 | Kiểm tra parse Excel bằng `xlsx` từ file người dùng upload (BOQ import, warranty import) — lib này có CVE (xem 9.2); đánh giá input không tin cậy đi vào parser | ✅ | **Xác nhận → F-14 (High, chưa vá — xử lý ở 9.2):** `xlsx@0.18.5` (bản npm cuối, không còn được vá trên registry) dính CVE-2023-30533 (prototype pollution khi parse file độc) + CVE-2024-22363 (ReDoS). Input KHÔNG tin cậy đi thẳng vào `XLSX.read(req.file.buffer)` tại 3 endpoint: [boqController.js:332](server/controllers/boqController.js#L332), [contractInBOQController.js:259](server/controllers/contractInBOQController.js#L259), [contractInDeliveryController.js:325](server/controllers/contractInDeliveryController.js#L325). Giảm nhẹ: sau requireAuth, giới hạn 5–10MB, memoryStorage — không loại trừ được CVE. Frontend cũng parse Excel client-side (warranty import) — rủi ro tự thân, thấp. |

## Giai đoạn 6 — CORS, CSRF & Security Headers ✅ Đã xong (F-15 đã vá; còn 1 việc xác minh tay trên VPS)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 6.1 | CORS ([server/index.js](server/index.js)): logic prod đã chặt ✓ — xác nhận giá trị `ALLOWED_ORIGINS` thực tế trên VPS không chứa origin lạ/wildcard | ✅ (local) / 🟡 (VPS) | Logic chuẩn: bật `credentials: true` + allowlist; production KHÔNG phản chiếu origin (`origin: false` khi thiếu `ALLOWED_ORIGINS` → chỉ same-origin), chỉ dev mới reflect-any. Không có `*` wildcard. `.env` local không đặt `ALLOWED_ORIGINS`/`NODE_ENV` (đúng cho dev, frontend prod phục vụ same-origin từ /dist). **Giá trị thực trên VPS phải kiểm tra tay** (thêm vào checklist VPS) — phải là origin app thật, không origin lạ. |
| 6.2 | CSRF: cookie `sameSite: 'lax'` chặn POST cross-site từ trình duyệt hiện đại — đánh giá còn lỗ nào (GET có side-effect? route nào dùng GET để thay đổi dữ liệu?) ; grep `router.get` có hành vi ghi | ✅ | Cookie phiên `sameSite: 'lax'` + `httpOnly` ✓ → request ghi cross-site (POST/PUT/DELETE) KHÔNG mang cookie. Grep toàn bộ `router.get` (≈45 route): **tất cả đều read-only** (`get*`, `download`, `view`, `lookup`, `*/template`) — không route GET nào ghi/sửa dữ liệu, nên `lax` (cho phép GET top-level navigation gửi cookie) không tạo vector CSRF. Không cần CSRF token. |
| 6.3 | Security headers tổng thể: chưa có helmet — đánh giá thêm `X-Content-Type-Options`, `X-Frame-Options`/CSP frame-ancestors (chống clickjacking), `Referrer-Policy`, CSP cơ bản cho SPA (có thể đặt ở nginx) | ✅ | **Phát hiện F-15 (Low), đã vá.** Trước đây chỉ `/uploads` có `nosniff`, không có chống clickjacking/CSP. Thêm middleware [securityHeaders.js](server/middleware/securityHeaders.js) áp mọi response: `nosniff` + `Referrer-Policy: no-referrer` (luôn bật); `X-Frame-Options: SAMEORIGIN` + CSP (chỉ production — dev frontend ở Vite :5173 nhúng iframe preview file từ Express :5174 là cross-origin, áp ở dev sẽ chặn nhầm). Tắt thêm `X-Powered-By`. CSP đã rà nới đúng nhu cầu thật: `style-src 'unsafe-inline'` (inline `style={{}}` khắp nơi), `script-src 'wasm-unsafe-eval'` + `worker-src blob:` (zxing-wasm), `frame-ancestors 'self'` (cho app tự nhúng iframe preview, vẫn chặn site ngoài — không dùng `'none'`/`DENY`). |
| 6.4 | SPA fallback `app.get('*splat')` ([server/index.js](server/index.js)) trả index.html cho mọi path — xác nhận không match nhầm `/api/*` hay `/uploads/*` đã 404 | ✅ | **Phát hiện vấn đề nhẹ, đã vá.** `/api/*` và `/uploads/*` không khớp trước đây `next()` rơi xuống `*splat` → trả index.html (HTML 200) thay vì 404 — che lỗi, dễ nhầm client gọi API (và file-not-found dưới `/uploads` trả HTML cho user đã đăng nhập). Thêm handler 404 JSON cho `['/api', '/uploads']` đặt TRƯỚC SPA fallback. Không phải lỗ bảo mật (API vẫn gated bởi `requireAuth` trong `apiRoutes`, SPA catch-all chỉ bắt phần còn dư) nhưng đúng hành vi hơn. |

**Bổ sung checklist VPS (từ 6.1):**

```bash
# 6.1 — ALLOWED_ORIGINS phải là origin app thật, không có origin lạ/wildcard
grep -E '^ALLOWED_ORIGINS=' .env    # vd https://app.tecapro.vn — KHÔNG để trống nếu frontend khác origin, KHÔNG chứa *
```

## Giai đoạn 7 — Rate limiting & DoS ✅ Đã xong (7.2 nới limit cho import, F-16 đã vá)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 7.1 | Hiện chỉ login có rate-limit — đánh giá các endpoint đắt khác: import Excel, upload 50MB, query báo cáo lớn (pmDashboard) có cần giới hạn không | ✅ | Quyết định **không thêm rate-limit chung**: mọi endpoint đắt đều sau `requireAuth` — bề mặt CHƯA đăng nhập chỉ còn login (đã limit) + `/api/health` (vá ở 7.3); với app nội bộ, user đã đăng nhập phá hoại là bài toán audit (Giai đoạn 10) chứ không phải rate-limit. Soát từng nhóm: parse `xlsx` CPU-bound + ReDoS thuộc F-14 (9.2); upload 50MB có multer limit per-request nhưng **không có quota theo user/đĩa** — chấp nhận, thêm việc theo dõi dung lượng `uploads/` vào checklist VPS; pmDashboard query tham số hóa, scoped theo đúng 1 PM (`requireSelfOrAdmin`, F-10) — chấp nhận. |
| 7.2 | `express.json()` không set limit → mặc định 100KB (Express 5) — xác nhận đủ, các endpoint nhận payload lớn (bulk BOQ) có bị chặn nhầm/cần limit riêng không | ✅ | Giữ mặc định 100KB toàn cục (đúng cho DoS). Nhưng **xác nhận có chặn nhầm**: 4 endpoint import nhận mảng JSON lớn từ Excel (`/contracts/:id/boq/save-import`, `/contract-ins/:id/boq/save-import`, `/contracts/:id/equipment/import`, `/contracts/:id/serials/import`) — file vài nghìn dòng/serial vượt 100KB là tình huống thật → 413 chặn nhầm. **Đã vá:** mount `express.json({ limit: '2mb' })` riêng 4 route đó TRƯỚC parser mặc định ([server/index.js](server/index.js)); an toàn vì body-parser bỏ qua request đã được parser trước đọc xong (đã xác minh `onFinished.isFinished` trong lib). Test thật: body 300KB vào cả 4 route import → 401 (qua parser, chặn bởi auth) ✓, vào route thường → 413 ✓. |
| 7.3 | `/api/health` public và query DB mỗi lần gọi — đánh giá khả năng bị spam làm cạn pool connection | ✅ | **Xác nhận → F-16 (Low), đã vá:** endpoint public, mỗi call chiếm 1 connection của `pg.Pool` (mặc định max 10) — spam đủ nhanh làm request thật phải xếp hàng. Rủi ro thực tế thấp (`select 1` rất rẻ, prod bind 127.0.0.1 sau nginx) nhưng vá rẻ: cache kết quả DB 5 giây, spam chỉ đập vào RAM. Test: 3 call liên tiếp trả `{"ok":true,"db":true}` ✓. |

**Bổ sung checklist VPS (từ 7.1 — upload không có quota theo user/đĩa):**

```bash
# 7.1 — theo dõi dung lượng thư mục uploads (không có quota per-user, mỗi file tới 50MB)
du -sh server/uploads/ && df -h .   # đảm bảo đĩa còn trống; cân nhắc cảnh báo khi đầy
```

## Giai đoạn 8 — Frontend ✅ Đã xong (không phát sinh finding mới)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 8.1 | XSS: đã grep nhanh không có `dangerouslySetInnerHTML`/`innerHTML`/`eval` ✓ — soát thêm chỗ render URL từ dữ liệu (href động, preview file iframe trong DocumentPreviewPanel) tránh `javascript:` URL | ✅ | Sạch. Grep lại toàn `src/`: không có `dangerouslySetInnerHTML`/`innerHTML`/`eval`/`new Function` ✓. Soát hết URL động: iframe/img preview + `window.open` đều là `${API}/files/${id}/view\|download` (origin cố định + id số từ DB) ✓; href tải template là hằng ✓. Duy nhất href từ dữ liệu: `att.file_path` ([TaskModal.jsx:240](src/components/contracts/TaskModal.jsx#L240), [TaskDeptGroup.jsx:97](src/components/contracts/TaskDeptGroup.jsx#L97)) — giá trị do **server tự ghi** (`/uploads/tasks/<id>/<timestamp>_<tên>`, [taskAttachmentController.js:55](server/controllers/taskAttachmentController.js#L55)), luôn bắt đầu `/uploads/` và tên file đã thay `:` → `_` nên không thể thành `javascript:` URL. Không có field URL nào do user nhập được render thành href. |
| 8.2 | Kiểm tra localStorage: CLAUDE.md nói còn lưu `userId` — xác nhận server không tin giá trị này (chỉ là cache UI), và không lưu token/data nhạy cảm khác trong localStorage | ✅ | localStorage **không còn lưu `userId`** — CLAUDE.md lỗi thời điểm này (sửa mô tả ở 12.3). Toàn `src/` chỉ còn 1 key `contractList.dateRange` (bộ lọc ngày, không nhạy cảm, parse trong try/catch ✓). Không dùng `sessionStorage`/`document.cookie`. Phiên khôi phục đúng chuẩn qua cookie httpOnly + `/api/auth/me` ([AuthContext.jsx](src/context/AuthContext.jsx)), client không tự khai id. |
| 8.3 | Kiểm tra [src/config/fetchSetup.js](src/config/fetchSetup.js) + [src/config/api.js](src/config/api.js): `credentials: 'include'` chỉ gửi tới API origin của mình, không leak cookie sang origin khác | ✅ | OK. Wrapper áp `credentials: 'include'` cho mọi fetch nhưng đã grep xác nhận **toàn bộ** lời gọi `fetch` trong `src/` đều trỏ `API`/`API_BASE`, không có fetch tới origin ngoài. Kể cả nếu sau này có, cookie `tecapro_auth` được trình duyệt scope theo origin API — `credentials: 'include'` không thể gửi nó sang origin khác (chỉ gửi cookie của chính origin đích). Wrapper spread `...init` sau nên caller vẫn override được khi cần ✓. |
| 8.4 | Kiểm tra ẩn UI theo role chỉ là UX — mọi kiểm soát thật nằm server-side (đối chiếu với kết quả Giai đoạn 3) | ✅ | Đúng như dự đoán: UI ẩn nút theo `user?.role == 1` ([QuantriPage.jsx](src/pages/QuantriPage.jsx) — thêm/sửa user, khách hàng, NCC). Đối chiếu server: user-management có `requireAdmin` thật ✓; nhưng customer/supplier POST/PUT ([customerRoutes.js](server/routes/customerRoutes.js)...) **không có** `requireAdmin` — ẩn nút chỉ là UX, user thường gọi API trực tiếp vẫn ghi được. Khớp đúng phát hiện 3.4, đã thuộc phạm vi **F-11** (quyết định mô hình quyền ở Giai đoạn 12) — không mở finding mới. |

## Giai đoạn 9 — Dependencies & supply chain ✅ Đã xong (F-14 đã vá; sau vá `npm audit` = 0 lỗ hổng)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 9.1 | Chạy `npm audit` (production deps), ghi lại các CVE high/critical và đường vá | ✅ | Production deps: chỉ 1 finding high = `xlsx` (đúng F-14, vá ở 9.2), không có CVE nào khác. Full audit (kèm dev) lộ thêm `shell-quote` **critical** (GHSA-w7jw-789q-3m8p) kéo theo bởi `concurrently@9` — dev-only, không vào bundle/prod, nhưng vá rẻ: **đã nâng `concurrently@10.0.3`** (CLI flags không đổi, `npx concurrently --version` OK). Sau cả 2 vá: `npm audit` = **0 vulnerabilities** (cả prod lẫn dev). |
| 9.2 | `xlsx@0.18.5`: có CVE đã biết (prototype pollution / ReDoS) và bản npm registry không còn được vá — đánh giá chuyển sang bản từ cdn.sheetjs.com hoặc lib thay thế (exceljs) vì app parse file Excel KHÔNG tin cậy do user upload | ✅ | **Đã vá F-14.** Chọn phương án giữ SheetJS, cài bản vá từ CDN chính chủ: `npm i https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` (0.20.3 > 0.20.2, vá cả CVE-2023-30533 lẫn CVE-2024-22363; không cần đổi code — exceljs sẽ phải viết lại 3 controller + 5 component). Cùng 1 package nên cover luôn frontend (các `await import('xlsx')` trong WarrantySerialSubTab, WarrantyEquipmentSubTab, DeliveryItemRow...). Đã test: roundtrip `write/read/sheet_to_json` Node OK (version 0.20.3), import cả 3 controller dùng xlsx OK, `npm run build` OK (chunk xlsx 493KB). Lưu ý: package.json giờ trỏ URL tgz — `npm install` trên VPS sẽ tự tải từ cdn.sheetjs.com; nâng cấp sau này phải đổi URL tay (không còn semver range). |
| 9.3 | Kiểm tra version Express 5 / multer 2 / pg đang dùng có advisory mở không | ✅ | `express@5.2.1`, `multer@2.1.1`, `pg@8.21.0` — cả 3 đều đang là bản **mới nhất** trên registry, `npm audit` không có advisory mở cho cả 3 (multer 2.x đã qua các bản vá DoS 2.0.1/2.0.2 từ lâu). Không cần hành động. |
| 9.4 | Gỡ dependency thừa (`bcrypt` hoặc `bcryptjs` — trùng chức năng, xem 2.7) | ✅ (đã làm ở 2.7 — gỡ `bcrypt`, giữ `bcryptjs`) | Xem 2.7 / F-09. |

## Giai đoạn 10 — Logging & xử lý lỗi ✅ Đã xong (F-17 đã vá; F-18 ghi nhận, chờ Giai đoạn 12)

| # | Bước | Trạng thái | Kết quả |
|---|------|-----------|---------|
| 10.1 | Review [server/utils/logger.js](server/utils/logger.js) + các điểm gọi: không log password, token, cookie, nội dung file | ✅ | Sạch. `logger.js` tối giản (timestamp + level, debug tắt ở prod) ✓. Grep toàn bộ ~150 điểm gọi `logger.*`/`console.*` trong `server/`: KHÔNG có chỗ nào log password/token/cookie/`req.body`/`req.headers`/nội dung file. [authController.js:69](server/controllers/authController.js#L69) chỉ log chuỗi tĩnh; error handler toàn cục log full `err` **phía server** (đúng chủ đích — pg error `detail` có thể chứa giá trị key nhưng chỉ vào log server, chấp nhận). [telegram.js](server/services/telegram.js) log body lỗi từ API Telegram, không log URL chứa bot token ✓. |
| 10.2 | Error handler toàn cục đã ẩn message ở prod ✓ — soát các controller có `res.status(500).json({ error: err.message })` cục bộ làm lộ chi tiết DB | ✅ | Handler toàn cục ([index.js:101](server/index.js#L101)) ẩn message ở prod đúng ✓. **Phát hiện F-17 (Low), đã vá:** 3 endpoint import trả `err.message` thô cho client bất kể môi trường — `importBOQPreview` ([boqController.js](server/controllers/boqController.js)), `importPurchaseBOQPreview` ([contractInBOQController.js](server/controllers/contractInBOQController.js)), `importDeliverySerials` ([contractInDeliveryController.js](server/controllers/contractInDeliveryController.js)). Nặng nhất là `importDeliverySerials`: rethrow lỗi Postgres từ transaction INSERT → lộ tên bảng/constraint. Đã thay bằng message chung, giữ `console.error` đầy đủ phía server. Các message 400/409 nghiệp vụ (serial trùng...) giữ nguyên — có chủ đích. |
| 10.3 | Đánh giá audit trail: hành động nhạy cảm (đổi quyền, xoá hợp đồng, xoá file) có ghi lại ai-làm-gì-khi-nào không | ✅ | **KHÔNG có audit trail → F-18 (Low/Medium, ghi nhận — quyết định ở Giai đoạn 12).** Không có bảng audit, không `logger.info` cho hành động ghi nào; chỉ có cột attribution rời rạc (`created_by`/`updated_by` trên `contract_out`, `customer`, task, document) — không cover DELETE (xoá HĐ/file/folder cascade không để lại dấu vết ai xoá), không log đổi quyền/đổi mật khẩu, không log đăng nhập (thành công lẫn thất bại) phía server — chỉ có Telegram báo cho chính chủ nếu user gắn chat_id. Càng đáng làm vì F-11 (mọi user sửa/xoá được mọi HĐ) — nên quyết cùng mô hình quyền ở Giai đoạn 12. |

## Giai đoạn 11 — Hạ tầng / triển khai VPS

| # | Bước | Trạng thái |
|---|------|-----------|
| 11.1 | HTTPS: nginx có redirect 80→443, cert còn hạn, HSTS | ⬜ |
| 11.2 | nginx có set `client_max_body_size` khớp limit 50MB; có truyền `X-Forwarded-For` đúng 1 hop (khớp `trust proxy 1`) | ⬜ |
| 11.3 | PostgreSQL trên VPS: chỉ listen localhost, password mạnh, có backup định kỳ (kèm `server/uploads/` — theo memory: uploads không nằm trong git, chỉ tồn tại trên VPS) | ⬜ |
| 11.4 | Quyền file hệ thống: `.env` chmod 600, process chạy bằng user thường (không root) | ⬜ |

## Giai đoạn 12 — Tổng hợp & vá

| # | Bước | Trạng thái |
|---|------|-----------|
| 12.1 | Tổng hợp findings, phân loại Critical / High / Medium / Low | ⬜ |
| 12.2 | Vá theo thứ tự ưu tiên (mỗi fix một commit riêng, migration nếu đụng schema theo quy ước `server/migrations/NNN_*.sql`) | ⬜ |
| 12.3 | Re-test sau vá (đăng nhập, upload, phân quyền) + cập nhật CLAUDE.md nếu có quy ước mới | ⬜ |

---

## Findings (ghi nhận trong quá trình review)

> Format: `ID [Mức độ] Mô tả — vị trí — trạng thái vá`

- **F-01 [Critical]** `TELEGRAM_BOT_TOKEN` lộ trong git history của repo **public** (`.env` tại commit `cfa456b` và các commit sau, đã xoá khỏi working tree ở `4685f69` nhưng history vẫn giữ). Ai cũng có thể lấy token và điều khiển bot (gửi tin nhắn giả mạo tới user, đọc update của bot). **Đã xác minh 2026-06-12: token VẪN SỐNG** (`getMe` trả về bot `TecaproCNHNbot` bình thường) → khắc phục là KHẨN CẤP. **Việc chỉ bạn làm được (theo thứ tự):** ① Mở Telegram → @BotFather → `/revoke` → chọn `TecaproCNHNbot` → nhận token mới → dán vào `.env` local (dòng `TELEGRAM_BOT_TOKEN=`) và `.env` trên VPS, restart app; ② Chuyển repo sang private: GitHub → repo `tecapro` → Settings → General → Danger Zone → "Change repository visibility" → Private (gh CLI không có trên máy nên không tự làm được); ③ (tuỳ chọn, sau khi revoke) dọn history bằng `git filter-repo` + force-push. — **① ✅ XONG (verify 2026-06-12: token cũ trong history trả 401 Unauthorized, token mới trong `.env` local hoạt động — nhớ cập nhật cả `.env` trên VPS + restart nếu chưa); ② 🟡 CHƯA — repo `vinhtdcnhn-stack/tecapro` VẪN PUBLIC (GitHub API trả 200, verify 2026-06-12); ③ tuỳ chọn**
- **F-02 [Medium]** `AUTH_SECRET` hiện 32 ký tự dạng human-pattern (kèm dấu backtick bao quanh trong file), yếu hơn khuyến nghị 48-byte random của chính codebase. Chưa từng lộ ra git. **Đã vá phía local (2026-06-12):** thay bằng 48-byte random hex mới (bỏ luôn backtick), server boot sạch — mọi phiên local cũ đã bị logout. **Còn VPS (bạn làm tay):** SSH vào VPS, chạy `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`, dán vào `AUTH_SECRET=` trong `.env` trên VPS (giá trị PHẢI KHÁC local), restart app — toàn bộ user sẽ phải đăng nhập lại. — **✅ local / 🟡 VPS**
- **F-03 [Low]** Password Postgres local là `123456` (user `postgres`), và cred localhost này cũng nằm trong history public. Chỉ rủi ro nếu máy dev/VPS mở port 5432 ra ngoài. **Khắc phục:** đổi password DB; xác nhận Postgres chỉ listen localhost (cả local lẫn VPS). — **chưa vá**
- **F-04 [Info]** `.env` local có khóa khai báo trùng (`DATABASE_URL` ×2, `VITE_API_BASE_URL` ×2) — dotenv lấy giá trị sau nên app vẫn chạy đúng DB `hello_web_db`, nhưng dễ gây nhầm khi sửa dòng đầu mà không có tác dụng. **Khắc phục:** xoá các dòng trùng, bổ sung `ALLOWED_ORIGINS`/`NODE_ENV` theo `.env.example`. — **chưa vá**
- **F-05 [Medium]** Token phiên sống 7 ngày và mang `role` ngay trong payload; `requireAuth` không đối chiếu DB → khi admin hạ quyền / xoá user / user đổi mật khẩu, token cũ vẫn hợp lệ với quyền cũ tới 7 ngày (denylist chỉ áp dụng khi tự logout). **Đã vá theo phương án ②+①:** thêm cột `app_user.token_version` (đã chạy trên DB local **và VPS** ✅ 2026-06-13; migration 042 đã được gộp vào baseline [server/schema.sql](server/schema.sql) và xoá khỏi `server/migrations/`); token nhúng claim `tv`; `requireAuth` đối chiếu DB mỗi request (role lấy giá trị mới nhất → đổi quyền hiệu lực ngay; `tv` phải khớp). Đổi mật khẩu (tự đổi hoặc admin reset qua updateUser) bump `token_version` → mọi phiên cũ logout; phiên tự đổi được cấp lại cookie nên không gián đoạn. Token cũ chưa có `tv` coi là 0 → deploy không làm logout hàng loạt. Đã test flow thật: login → đổi mật khẩu → token cũ 401, token mới 200. — **✅ đã vá** (commit `cd6340a`)
- **F-06 [Medium]** Express bind `0.0.0.0` kết hợp `trust proxy 1`: nếu VPS không chặn port 5174 từ ngoài, attacker gọi thẳng Express bỏ qua nginx và giả `X-Forwarded-For` để xoay IP → bypass login rate-limit. **Đã vá:** production mặc định bind `127.0.0.1` ([server/index.js](server/index.js)), override được bằng env `HOST` (đã ghi chú trong `.env.example`). Vẫn nên xác nhận trên VPS sau khi deploy: `ss -tlnp | grep 5174` phải ra `127.0.0.1:5174`. — **✅ đã vá** (commit `14441cc`)
- **F-07 [Low]** Mật khẩu mới chỉ yêu cầu tối thiểu 6 ký tự; `createUser`/`updateUser` (admin) không kiểm tra độ dài nào. **Đã vá:** tối thiểu 8 ký tự (`PASSWORD_MIN_LENGTH`), áp dụng thống nhất cả create/update/change-password phía server + đồng bộ validate phía client (`ChangePasswordModal`). Mật khẩu cũ ngắn hơn vẫn đăng nhập được, chỉ chặn khi đặt mới. — **✅ đã vá** (commit `af5a58f`)
- **F-08 [Info]** Login có timing oracle nhẹ: email không tồn tại trả lời ngay, email đúng phải chạy bcrypt → đo thời gian suy ra email tồn tại. **Đã vá:** nhánh không tìm thấy user chạy `bcrypt.compare` với dummy hash để hai nhánh tốn thời gian như nhau. — **✅ đã vá** (commit `0dbf8a8`)
- **F-09 [Info]** Dependency trùng chức năng: cả `bcrypt` lẫn `bcryptjs` trong package.json, nhưng source chỉ import `bcryptjs`. **Khắc phục:** đã chạy `npm uninstall bcrypt` (gỡ 3 packages, package.json + lockfile đã cập nhật). — **✅ đã vá** (đồng thời đóng luôn bước 9.4)
- **F-10 [Medium]** IDOR PM dashboard: `GET /api/pm/:userId/dashboard` và `PUT /api/pm/:userId/tracking` lấy `userId` từ URL không có guard — user thường xem được toàn cảnh HĐ/công nợ/công việc của PM khác và ghi đè ghim/nhắc của họ. **Đã vá:** thêm `requireSelfOrAdmin('userId')` cho cả 2 route ([pmDashboardRoutes.js](server/routes/pmDashboardRoutes.js)); frontend chỉ gọi bằng id của chính mình nên không ảnh hưởng. — **✅ đã vá** (commit `36ee2b7`)
- **F-11 [High]** Thiếu kiểm tra membership trên hầu hết tài nguyên hợp đồng: chỉ DANH SÁCH HĐ bán được lọc theo PM ([contractController.getAllContracts](server/controllers/contractController.js)); còn `GET/PUT /contracts/:id` và toàn bộ tài nguyên con — BOQ, receivable, progress, guarantee, document folder/file (gồm `/api/files/:id/view`/download/delete), task, contract-in cùng mọi bảng con, serial/warranty — chỉ cần đăng nhập là đọc/sửa/xoá theo id bất kỳ. Leo thang dễ nhất: user thường gọi `PUT /contracts/:id` thay danh sách thành viên để tự thêm mình làm PM. **Mô hình quyền đã chốt: chỉ PM của HĐ (`contract_out_member.member_role = 'PM'`, gồm PM chính + đồng PM) hoặc admin (`role=1`) mới được GHI/SỬA/XOÁ; ĐỌC (GET) vẫn mở cho mọi user đăng nhập (tra cứu chéo có chủ đích).** **Đã vá:** middleware [contractAccess.js](server/middleware/contractAccess.js) với 3 helper — `pmFromParam` (URL chứa id HĐ bán), `pmVia(key,param)` (id con → JOIN ngược về `contract_out`/`contract_in` qua bảng RESOLVERS), `pmViaBody(key)` (bulk `{ids:[...]}`); bulk yêu cầu PM trên TẤT CẢ HĐ liên quan. Áp cho mọi route `post/put/delete` thuộc hợp đồng trong 19 router (gồm `PUT /contracts/:id` chặn tự thêm PM, `/api/files/:id/view`/download/delete, replaceSerial/replaceDeliverySerial). Guard đặt TRƯỚC multer ở route upload. Ngoài hợp đồng, danh mục dùng chung (customer/supplier/bb-types) trước cũng mở cho mọi user → siết `requireAdmin` cho create/update/delete ([customerRoutes.js](server/routes/customerRoutes.js), [supplierRoutes.js](server/routes/supplierRoutes.js), [progressRoutes.js](server/routes/progressRoutes.js)) + ẩn nút "Quản lý loại BB" với non-admin trong 2 tab tiến độ. Giữ ngoại lệ có chủ đích cho `/warranty-lookup` (tra cứu cross-contract) và mọi GET. Frontend: `ContractPermContext`/`EditGuard`/`useCanEdit` ẩn nút ghi với non-PM (UX; kiểm soát thật ở server). Build `vite build` OK. — **✅ đã vá** (commit `2821162` + siết danh mục dùng chung)
- **F-12 [Medium]** Upload blocklist sót các đuôi trình duyệt render như HTML/SVG: `.shtml`, `.xht`, `.svgz` (mime-db map về `text/html` / `image/svg+xml`) → user đăng nhập upload được file chứa script; nạn nhân mở qua `/uploads/...` hoặc `/api/files/:id/view` là script chạy same-origin (stored XSS — gọi API bằng cookie phiên của nạn nhân, nosniff không chặn được vì Content-Type đúng là html). **Đã vá:** thêm 3 đuôi vào blocklist ([uploadFilter.js](server/middleware/uploadFilter.js)); `viewFile` ép tải về (`res.download`) mọi file có đuôi thuộc blocklist để vô hiệu cả file cũ đã upload trước khi có filter. — **✅ đã vá** (commit `3db4b3f`)
- **F-13 [Medium]** Path traversal khi lưu upload: `contractId`/`contractInId`/`taskId` từ URL không ép kiểu số mà nối thẳng vào multer destination; Express decode `%2F` trong param nên `..%2F..%2F` tạo được thư mục (và ghi file tạm) ngoài `server/uploads/`. **Đã vá:** ép `^\d+$` trong cả 3 destination ([documentController.js](server/controllers/documentController.js), [taskAttachmentController.js](server/controllers/taskAttachmentController.js)); sanitize phần extension của tên file; hardening chiều đọc/xoá: `resolveFilePath` + `deleteAttachment` normalize và chặn mọi đường dẫn thoát khỏi `uploads/`. — **✅ đã vá** (commit `3db4b3f`)
- **F-15 [Low]** Thiếu security headers tổng thể: trước đây chỉ `/uploads` có `nosniff`, không có chống clickjacking (`X-Frame-Options`/CSP `frame-ancestors`), `Referrer-Policy`, hay CSP — app dễ bị nhúng iframe (clickjacking) và không có lớp phòng vệ XSS bổ sung. Lộ thêm `X-Powered-By: Express`. **Đã vá:** middleware [securityHeaders.js](server/middleware/securityHeaders.js) áp mọi response (`nosniff` + `Referrer-Policy: no-referrer` luôn bật; `X-Frame-Options: SAMEORIGIN` + CSP ở production — gate prod vì preview file ở dev là cross-origin :5173→:5174); CSP nới đúng nhu cầu (`style-src 'unsafe-inline'`, `script-src 'wasm-unsafe-eval'`/`worker-src blob:` cho zxing-wasm, `frame-ancestors 'self'` cho iframe preview của chính app). Tắt `X-Powered-By`. Kèm sửa SPA fallback: thêm 404 JSON cho `/api`+`/uploads` không khớp (trước trả index.html HTML 200). CSP chặt hơn (hash/nonce, bỏ `'unsafe-inline'`) để dành cho nginx. — **✅ đã vá**
- **F-16 [Low]** `/api/health` public và query DB (`select 1`) mỗi lần gọi — spam endpoint này chiếm connection của `pg.Pool` (mặc định max 10), làm request thật phải chờ. Giảm nhẹ sẵn có: query rất rẻ, production bind `127.0.0.1` sau nginx. **Đã vá:** cache kết quả DB 5 giây trong RAM ([server/index.js](server/index.js)) — spam chỉ đọc cache, DB bị chạm tối đa 1 lần/5s. Kèm theo (7.2, không phải lỗ hổng): nới `express.json` limit 2MB riêng cho 4 route import JSON lớn, giữ 100KB toàn cục. — **✅ đã vá**
- **F-14 [High]** `xlsx@0.18.5` (bản cuối trên npm registry, SheetJS không vá trên npm nữa) dính CVE-2023-30533 (prototype pollution khi parse file Excel độc) và CVE-2024-22363 (ReDoS); app parse file user upload tại 3 endpoint import BOQ/serial (sau đăng nhập, giới hạn 5–10MB — giảm nhẹ, không loại trừ). **Đã vá (9.2, 2026-06-12):** cài `xlsx@0.20.3` từ CDN chính chủ (`npm i https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`) — vá cả 2 CVE, không đổi code (API `read`/`utils`/`writeFile` giữ nguyên), cùng package nên frontend (dynamic `import('xlsx')`) dùng chung bản vá. Đã test roundtrip Node + import 3 controller + `vite build`. `npm audit --omit=dev` = 0. — **✅ đã vá**
- **F-17 [Low]** 3 endpoint import trả `err.message` thô cho client bất kể môi trường (vượt qua cơ chế ẩn lỗi prod của handler toàn cục): `importBOQPreview`, `importPurchaseBOQPreview` (lộ message parser xlsx) và `importDeliverySerials` (rethrow lỗi Postgres từ transaction → lộ tên bảng/constraint, vd FK `contract_in_delivery_serial`). **Đã vá:** thay bằng message chung thân thiện, giữ nguyên `console.error(err)` đầy đủ phía server; không đụng các message 400/409 nghiệp vụ. — **✅ đã vá**
- **F-18 [Low/Medium]** Không có audit trail: hành động nhạy cảm (đổi quyền, reset mật khẩu, xoá hợp đồng/file/folder, login thành công/thất bại) không được ghi lại ai-làm-gì-khi-nào ở server. Chỉ có cột `created_by`/`updated_by` trên vài bảng (không cover DELETE). Rủi ro khuếch đại bởi F-11 (mọi user đăng nhập sửa/xoá được mọi HĐ — không truy được thủ phạm). **Hướng xử lý (Giai đoạn 12, quyết cùng mô hình quyền F-11):** tối thiểu là `logger.info` có cấu trúc (user id, action, resource, id) cho login + các op DELETE/đổi quyền; đầy đủ hơn là bảng `audit_log` (migration theo quy ước `server/migrations/NNN_*.sql`). — **chưa vá (ghi nhận, chờ Giai đoạn 12)**

## Ghi chú đã biết trước khi review (từ khảo sát Giai đoạn 0)

1. **`xlsx@0.18.5`** parse file user upload + có CVE công khai → ứng viên finding High (xem 5.6, 9.2).
2. **Upload filter là blocklist** không phải allowlist, chưa check MIME (xem 5.1).
3. **Denylist logout token chỉ trong RAM** — mất khi restart, không chia sẻ đa instance (đã ghi chú trong code là chấp nhận; xem 2.6).
4. **Cả `bcrypt` và `bcryptjs`** cùng có mặt trong dependencies (xem 2.7).
5. **Chưa có security headers tổng thể** (helmet/CSP) ngoài `nosniff` cho `/uploads` (xem 6.3).
6. **Token 7 ngày không tự vô hiệu khi đổi role/khoá user** (xem 2.1).
